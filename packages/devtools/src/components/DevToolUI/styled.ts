import { styled } from '@tabulus/utils';

const DevToolIcon = styled.button`
  background-color: '#00C7AC';
  color: '#000000';
  padding: 2px;
`;

const DevToolContainer = styled.div`
  position: absolute;
`;

export { DevToolContainer, DevToolIcon };

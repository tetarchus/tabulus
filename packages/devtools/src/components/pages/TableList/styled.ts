import { styled } from '@devtools/utils';

/**
 * Wrapper for the whole table list page.
 */
const TableListWrapper = styled.div`
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  /* gap: 0.25rem; */
  height: 100%;
`;

export { TableListWrapper };

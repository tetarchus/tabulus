import styled from '@emotion/styled';

const AlignmentIconOptions = styled.div`
  display: flex;
  gap: 0.5rem;
  height: 2rem;
  justify-content: center;
`;

const SettingsWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
`;

export { AlignmentIconOptions, SettingsWrapper };

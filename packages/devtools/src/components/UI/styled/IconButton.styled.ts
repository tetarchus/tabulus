import styled from '@emotion/styled';

const IconButton = styled.button`
  background-color: transparent;
  color: inherit;
  border: none;
  cursor: pointer;
  padding: 0.12rem;
  &:hover {
    opacity: 0.8;
  }
`;

export { IconButton };

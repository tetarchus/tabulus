import { styled } from '@tabulus/utils';

import type { FooterProps } from './types';

/** Container for the table footer contents. */
const Footer = styled.div<FooterProps>``;

export { Footer };
export type { FooterProps } from './types';

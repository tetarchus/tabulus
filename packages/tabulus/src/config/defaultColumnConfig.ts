import type { FullColumnConfig } from '@tabulus/types';

const defaultColumnConfig: FullColumnConfig = {
  //== General ========================
  visible: true,
  //== Layout =========================
  horizontalAlign: 'left',
  verticalAlign: 'middle',
  headerHorizontalAlign: 'left',
  headerVerticalAlign: 'middle',
};

export { defaultColumnConfig };

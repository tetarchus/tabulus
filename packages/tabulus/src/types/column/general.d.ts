/** General column options. */
interface GeneralColumnOptions {
  /**
   * Whether the column should be visible in the table.
   * @default true
   */
  visible: boolean;
}

export { GeneralColumnOptions };

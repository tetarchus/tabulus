/** Table options relating to rows/row data. */
interface TableRowOptions {
  /**
   * Whether to allow the user to re-order rows by clicking and dragging.
   * @default false
   */
  movableRows: boolean;
}

export type { TableRowOptions };

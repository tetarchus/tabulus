import { describe, /*  expect, */ it } from 'vitest';

// import { setTableTheme } from '..';

describe('Theme Tests', () => {
  it.todo('returns the selected theme when a string value is passed');

  it.todo('returns an assembled theme when a custom theme is passed');
});

import type { Theme } from '@tabulus/types';

/** The default theme for `Tabulus` tables. */
const standard: Theme = {
  borders: {},
  colors: {
    backgroundColor: '#CCC',
    textColor: '#000',
  },
};

export { standard };

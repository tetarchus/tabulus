// TODO: Remove these if not required

// const isDefinedRef = <T>(ref: MutableRefObject<T | undefined>): ref is MutableRefObject<T> =>
//   ref.current != null;

// const isHeaderRow = <RowData extends SimpleRowData>(
//   row: RowComponent<RowData>,
// ): row is RowComponentHeader & RowComponentBase<RowData> => row.type === 'header';

// const isTableRow = <RowData extends SimpleRowData>(
//   row: RowComponent<RowData>,
// ): row is RowComponentStandard<RowData> & RowComponentBase<RowData> => row.type === 'table';

export {};

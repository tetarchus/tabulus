# useTabulusRegistry

The `useTabulusRegistry` hook allows you to access tables from anywhere below the
`<TabulusRegistry>` context provider, allowing you to access/change their data from outside of their
tree.
